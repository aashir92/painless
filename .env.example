# Set in Cloudflare Pages → Settings → Environment variables (Production + Preview)
# Use your real domain or *.pages.dev URL so metadata (Open Graph, canonical) is correct.
NEXT_PUBLIC_SITE_URL=https://painless.pages.dev
